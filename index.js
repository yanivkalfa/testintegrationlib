/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './react-app/App';
import {name as appName} from './react-app/app.json';

AppRegistry.registerComponent(appName, () => App);
