import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
  },
  infoContainer: {
    marginTop: 16,
    alignItems: 'center',
  },
  text: {
    fontSize: 16,
    color: 'black',
  },
  error: {
    color: 'red',
    marginTop: 16,
    fontSize: 14,
  },
});
