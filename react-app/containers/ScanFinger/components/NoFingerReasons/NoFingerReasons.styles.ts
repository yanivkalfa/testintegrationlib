import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  noReasonButton: {
    flex: 1,
    marginRight: 5,
    paddingVertical: 5,
    alignItems: 'center',
    justifyContent: 'center',
  },
  noReasonButtonText: {
    fontSize: 12,
  },
});
